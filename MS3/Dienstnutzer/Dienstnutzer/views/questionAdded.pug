extends layout

block content

    script(type='text/javascript').


            showEditEntry();
            console.log(!{JSON.stringify(qText)})
            let qText = !{JSON.stringify(qText)};
            let a1 = !{JSON.stringify(a1)};
            let a2 = !{JSON.stringify(a2)};
            let a3 = !{JSON.stringify(a3)};
            let a4 = !{JSON.stringify(a4)};
            let ra = !{JSON.stringify(ra)};

            let overlay = document.getElementById("userInput")
            let div = document.createElement("div");
            div.style.padding = "1rem";
            div.style.marginLeft = "1rem";
            div.style.marginTop = "1rem";
            div.style.marginBottom = "1rem";
            div.style.height = "auto";
            div.style.color = "white";
            div.style.fontFamily = "Orbitron"
            div.innerHTML = "Text: " + qText + "<br />" + "Answer 1 : " + a1 + "<br />" + " \nAnswer 2 : " + a2 + "<br />" + "\nAnswer 3 : " + a3 + "<br />" + "\nAnswer 4 : " + a4 + "<br />" + "\nRight Answer : " + ra;
            overlay.appendChild(div);

    ul
        li
            a.active(href='/') Login
        li
            a(href='/dashboard') Dashboard
        li
            a(href='/question') question
        li
            a(href='/quiz') quiz
        li
            a(href='/more') More



    .maincontent
